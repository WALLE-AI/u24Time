# 端对端多智能体情报预测架构方案
> 基于 BettaFish + MiroFish + OpenClaw 三方项目源码深度分析
> 更新时间: 2026-03-08 | 分析深度: 源码级

---

## 一、三大项目核心技术地图

### 1.1 BettaFish — 舆情分析管线核心技术

| 模块 | 核心文件 | 关键实现细节 |
| :--- | :--- | :--- |
| **ForumEngine** | [monitor.py](file:///d:/WALL-AI/u24Time/opensource/BettaFish/ForumEngine/monitor.py) / [llm_host.py](file:///d:/WALL-AI/u24Time/opensource/BettaFish/ForumEngine/llm_host.py) | [LogMonitor](file:///d:/WALL-AI/u24Time/opensource/BettaFish/ForumEngine/monitor.py#24-838) 轮询 3 个 [.log](file:///d:/WALL-AI/u24Time/backend/error.log) 文件; 每采集到 **5 条** Agent 发言触发 [ForumHost](file:///d:/WALL-AI/u24Time/backend/agents/phase1_bettafish/forum/host.py#12-59)(Qwen 模型) 生成引导总结; 用 `threading.Lock` 保证并发安全写入 `forum.log` |
| **InsightEngine** | [agent.py](file:///d:/WALL-AI/u24Time/opensource/BettaFish/InsightEngine/agent.py) (40KB)  | `search_hot_content / search_topic_globally / get_comments_for_topic` 等 5 大查询工具; UNION ALL 跨 15 张表; 加权热度: Like×1 + Comment×5 + Share×10 + View×0.1 |
| **QueryEngine** | `nodes/` | SearchNode → FormattingNode → **ReflectionSummaryNode**(max 2 轮) → FirstSummaryNode → SummaryNode; 最多 2 轮反思补缺; max_search_results=15 |
| **SentimentAnalyzer** | `SentimentAnalysisModel/` | 默认用 `tabularisai/multilingual-sentiment-analysis`(22 语言); 5 级: 0-非常负面 ～ 4-非常正面; 批量处理返回情感分布/置信度均值 |
| **ReportEngine** | `ReportEngine/` | 模板选择 → 布局设计 → 字数预算 → 逐章生成 → JSON 校验 → 装订 → HTML/PDF/MD |

### 1.2 MiroFish — 图谱仿真预测核心技术

| 模块 | 关键实现细节 |
| :--- | :--- |
| **OntologyGenerator** | temperature=0.3, 精确 10 实体 + 6-10 关系; 最后 2 个必须是 Person/Organization; 禁止 Zep 保留字 (uuid/name/summary/...) |
| **GraphBuilderService** | Zep Cloud API; chunk_size=500 overlap=50; batch_size=3 写入 EpisodeData; 轮询等待 episode.processed (超时 600s) |
| **OasisProfileGenerator** | 从图谱节点生成 Agent 人设/记忆/行为配置 |
| **SimulationRunner** | OASIS 双平台并行 (Twitter + Reddit); 默认 144 轮 (72 虚拟小时); Agent 动作实时写入 Zep 图谱记忆 (valid_at/invalid_at 时序边) |
| **ReportAgent** | 99KB 核心服务文件; 查询图谱 + 与模拟 Agent 深度对话 + 生成预测报告 |
| **ZepTools** | 实体检索/关系路径查询/时序事实查询/Agent记忆读取 |

### 1.3 OpenClaw — 智能体运行时核心技术

| 技术域 | 核心文件 | 关键实现细节 |
| :--- | :--- | :--- |
| **ContextEngine (接口)** | [context-engine/types.ts](file:///d:/WALL-AI/u24Time/opensource/openclaw/src/context-engine/types.ts) | 定义 `bootstrap/ingest/ingestBatch/afterTurn/assemble/compact/prepareSubagentSpawn/onSubagentEnded`; `isHeartbeat` 标志区分心跳与常规 Turn |
| **记忆管理** | [memory/manager.ts](file:///d:/WALL-AI/u24Time/opensource/openclaw/src/memory/manager.ts) | [MemoryIndexManager](file:///d:/WALL-AI/u24Time/opensource/openclaw/src/memory/manager.ts#45-787): sqlite + sqlite-vec + BM25 FTS 混合检索; 支持 OpenAI/Gemini/Voyage/Mistral/Ollama 多嵌入模型自动 fallback; [search()](file:///d:/WALL-AI/u24Time/opensource/openclaw/src/memory/manager.ts#240-349) 方法实现 Hybrid BM25+Vector + MMR + 时序衰减 |
| **子智能体注册表** | [agents/subagent-registry.ts](file:///d:/WALL-AI/u24Time/opensource/openclaw/src/agents/subagent-registry.ts) | `SubagentRunRecord` 完整生命周期; 孤儿子进程清理; **指数退避** 重试公告 (1s→2s→4s→8s); Sweeper 每 60s 循环清档; `archiveAfterMinutes` 控制存档超时 |
| **心跳调度** | [cron/heartbeat-policy.ts](file:///d:/WALL-AI/u24Time/opensource/openclaw/src/cron/heartbeat-policy.ts) | [shouldSkipHeartbeatOnlyDelivery()](file:///d:/WALL-AI/u24Time/opensource/openclaw/src/cron/heartbeat-policy.ts#9-30) 判断是否抑制纯心跳输出; [shouldEnqueueCronMainSummary()](file:///d:/WALL-AI/u24Time/opensource/openclaw/src/cron/heartbeat-policy.ts#31-49) 控制心跳任务汇报; `isHeartbeat` 标志贯穿 Context Engine |
| **渠道网关** | `channels/` | [registry.ts](file:///d:/WALL-AI/u24Time/opensource/openclaw/src/channels/registry.ts) 统一渠道注册; [dock.ts](file:///d:/WALL-AI/u24Time/opensource/openclaw/src/channels/dock.ts) 双向消息调度; [draft-stream-loop.ts](file:///d:/WALL-AI/u24Time/opensource/openclaw/src/channels/draft-stream-loop.ts) SSE 流式播放; [inbound-debounce-policy.ts](file:///d:/WALL-AI/u24Time/opensource/openclaw/src/channels/inbound-debounce-policy.ts) 防抖 (避免频繁触发); 支持 Telegram/WhatsApp/Slack/Discord 等 |

---

## 二、E2E 整合架构设计

### 2.1 五层架构模型

```
┌──────────────────────────────────────────────────────────────────┐
│  LAYER 5: 渠道网关层 (Channel Gateway)                            │
│  FastAPI WebSocket + SSE 双向网关; 外部 Webhook; 前端 UI 实况推送  │
└──────────────────────────┬───────────────────────────────────────┘
                           │
┌──────────────────────────▼───────────────────────────────────────┐
│  LAYER 4: 心跳与调度层 (Heartbeat & Scheduler)                    │
│  APScheduler 后台任务; 防重入锁; 心跳探测; 任务生命周期管理          │
└──────────────────────────┬───────────────────────────────────────┘
                           │ 触发
┌──────────────────────────▼────────────────────────────────────────────────┐
│  LAYER 3: 多智能体协作层 (Multi-Agent Coordination)                        │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │ SubagentRegistry (参考 OpenClaw)                                    │  │
│  │  ├── CrawlerAgent (TopicExtractor → SocialCrawler)                  │  │
│  │  ├── InsightAgent (MediaCrawlerDB × 5工具 + 热度计算)               │  │
│  │  ├── QueryAgent   (SearchNode→Reflect→Summary, max 2 轮)            │  │
│  │  ├── MediaAgent   (多模态内容分析)                                  │  │
│  │  ├── SentimentAgent (22语言5级情感分析)                             │  │
│  │  ├── ForumHost    (5条发言触发 → Qwen LLM 引导 → forum.log)         │  │
│  │  ├── OntologyAgent (10实体+6-10关系本体设计)                        │  │
│  │  ├── GraphAgent   (Zep Cloud 图谱构建 + 时序边)                     │  │
│  │  ├── SimulationAgent (OASIS 144轮 Twitter+Reddit 双平台)             │  │
│  │  └── PredictionAgent (ReportAgent 图谱查询+Agent对话+预测报告)       │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
└──────────────────────────┬────────────────────────────────────────────────┘
                           │
┌──────────────────────────▼───────────────────────────────────────┐
│  LAYER 2: 上下文与记忆层 (Context & Memory)                        │
│  ContextEngine (bootstrap/ingest/compact/assemble)               │
│  + MemoryIndexManager (sqlite-vec + BM25 Hybrid)                 │
│  + Zep Cloud (时序图谱长期记忆)                                   │
└──────────────────────────┬───────────────────────────────────────┘
                           │
┌──────────────────────────▼───────────────────────────────────────┐
│  LAYER 1: 数据基础层 (Data Foundation)                             │
│  7平台×14表原始数据; canonical_items; daily_topics; 向量 DB        │
└──────────────────────────────────────────────────────────────────┘
```

### 2.2 E2E 完整数据流

```
[心跳调度] ──定时触发──▶ [Phase 0: 感知采集]
                             TopicExtractor (BroadTopicExtraction)
                             SocialCrawler (7平台 × 14表)
                             ↓
                         [Phase 1: 数据对齐]
                             MediaCrawlerDB 5大查询工具
                             加权热度计算 (Like×1+Comment×5+Share×10)
                             HotnessThreshold 触发判定
                             ↓ 阈值超出
                         [Phase 2: 并行分析] ← ContextEngine 维持会话
                             QueryAgent  ──┐
                             InsightAgent──┼──▶ ForumHost (每5条发言LLM引导)
                             MediaAgent  ──┘    共享 forum.log
                             SentimentAgent (情感分析批处理)
                             ↓
                         [Phase 3: 选题与快报]
                             智能选题 (热度 × 情感波动 × 事件重要性)
                             ReportEngine 初始快报 (HTML/MD)
                             ↓
                         [Phase 4: 图谱重建]
                             OntologyGenerator (精确10实体+6-10关系)
                             GraphBuilderService → Zep Cloud
                             ↓
                         [Phase 5: 仿真推演]
                             OasisProfileGenerator (人设生成)
                             SimulationRunner (144轮 双平台并行)
                             ZepGraphMemoryUpdater (在线更新图谱边)
                             ↓
                         [Phase 6: 预测研判]
                             PredictionReportAgent (图谱查询+Agent对话)
                             结果存入 MemoryIndexManager (长期记忆)
                             ↓
                         [Channel Gateway 分发]
                             WebSocket Push → 前端 UI
                             Webhook → Telegram / 外部系统
```

---

## 三、核心技术对应映射

### 3.1 上下文管理 (Context Management)
- **会话生命周期**: 参照 OpenClaw [ContextEngine](file:///d:/WALL-AI/u24Time/opensource/openclaw/src/context-engine/types.ts#67-168) 接口，为每个 Pipeline 运行维护 `sessionId`，实现 `bootstrap/ingest/compact` 三个生命周期钩子
- **Token 保护**: 借鉴 OpenClaw `compaction` 机制，Forum 协作产生的长尾对话自动裁减，保留高信息密度段落
- **心跳标志**: `isHeartbeat=True` 区分定时触发与手动触发，心跳触发产物不推送到用户界面

### 3.2 记忆管理 (Memory Management)
- **短期**: [AgentsSessionModel](file:///d:/WALL-AI/u24Time/backend/db/models.py#199-217) 维护当前 Pipeline 运行状态和中间结果
- **长期 (分析历史)**: 模拟 OpenClaw [MemoryIndexManager](file:///d:/WALL-AI/u24Time/opensource/openclaw/src/memory/manager.ts#45-787)，将分析报告切片存入 SQLite (BM25 FTS + Python 侧 vector 搜索)
- **图谱记忆**: Zep Cloud 时序图谱，`valid_at/invalid_at` 建模知识有效期，为下次同话题分析提供先验知识

### 3.3 心跳技术 (Heartbeat)
- **调度器**: `APScheduler` AsyncIO 模式，可配置 cron 触发间隔 (默认 6 小时)
- **防重入**: 心跳触发前检查 `is_running` 标志，确保同一 Pipeline 不并发执行
- **自愈**: 子任务失败触发指数退避重试 (参考 OpenClaw [resolveAnnounceRetryDelayMs](file:///d:/WALL-AI/u24Time/opensource/openclaw/src/agents/subagent-registry.ts#115-122))
- **清档**: 完成 Task 后由 Sweeper 定期清理历史运行记录 (参考 SubagentRegistry sweepSubagentRuns)

### 3.4 渠道技术 (Channel Technology)
- **内部流**: FastAPI SSE `/agents/stream/{run_id}` 向前端 UI 实时推送进度
- **外部推送**: 参考 OpenClaw [channels/dock.ts](file:///d:/WALL-AI/u24Time/opensource/openclaw/src/channels/dock.ts)，实现 Webhook 抽象，支持 Telegram/企业微信
- **防抖策略**: 参考 OpenClaw [inbound-debounce-policy.ts](file:///d:/WALL-AI/u24Time/opensource/openclaw/src/channels/inbound-debounce-policy.ts)，短时间内多次触发仅执行最后一次

---

## 四、分阶段执行方案 (Execution Plan)

> 现有基础: `backend/agents` 已实现 BettaFish P0-P3 API 路由 + MiroFish P4-P5 框架

### Phase 1 — 基础设施层完善 (预计 2-3 天)

#### 1.1 心跳调度引擎
**新建**: `backend/agents/scheduler.py`
```python
# 核心设计:
# - APScheduler AsyncIO scheduler
# - 心跳任务: trigger='cron', hour='*/6'
# - 防重入: asyncio.Lock per topic
# - 状态持久化: AgentsSessionModel (running/completed/failed)
# - 指数退避重试 (1s, 2s, 4s, 8s)
```

#### 1.2 长效记忆连接器
**新建**: `backend/agents/memory.py`
```python
# 核心设计:
# - SQLite + sqlite-vec (轻量向量) 用于历史报告检索
# - 接口: store_analysis(topic, content), search_history(query, k=5)
# - 与 AgentsSessionModel 联动写入
# - Zep 图谱读取接口 (先验知识检索)
```

#### 1.3 上下文引擎
**新建**: `backend/agents/context_engine.py`
```python
# 核心设计:
# - AgentContext: sessionId, messages[], tokenCount
# - bootstrap(): 读取历史先验记忆注入 System Prompt
# - ingest(): 追加 Agent 消息
# - compact(): Token 超限时自动裁减 (保留最近 N 条)
# - isHeartbeat 标志控制输出策略
```

### Phase 2 — E2E 主控协调器 (预计 3-4 天)

#### 2.1 Pipeline 协调器
**新建**: `backend/agents/pipeline/e2e_coordinator.py`
```python
# 核心设计:
# - EndToEndCoordinator.run(topic, trigger='heartbeat'|'manual')
# - stage_1_acquire(): TopicExtractor + SocialCrawler
# - stage_2_align(): MediaCrawlerDB + HotnessCalculator → 阈值判断
# - stage_3_analyze(): asyncio.gather(QueryAgent, InsightAgent, MediaAgent)
#   ForumHost 在 5 条消息后自动触发 (参考 BettaFish host_speech_threshold)
# - stage_4_sentiment(): SentimentAgent 批量处理
# - stage_5_select_topic(): 热度 × 情感 × 事件权重排序选题
# - stage_6_report(): ReportEngine 初始快报
# - stage_7_graph(): OntologyGenerator + GraphBuilder (Zep)
# - stage_8_simulate(): SimulationRunner (144 轮)
# - stage_9_predict(): PredictionReportAgent
# - 完成后写入 MemoryIndexManager 长期记忆
```

#### 2.2 子智能体注册表
**新建**: `backend/agents/subagent_registry.py`
```python
# 参考 OpenClaw subagent-registry.ts
# - SubagentRunRecord: runId, stage, status, startedAt, endedAt
# - register()/complete()/fail() 状态流转
# - Sweeper 定期清理 (asyncio.create_task)
```

### Phase 3 — 渠道网关层 (预计 1-2 天)

#### 3.1 WebSocket 实况推送
**更新**: `backend/agents/server.py` 新增 WebSocket endpoint
```python
# /agents/ws/{run_id}: 推送 E2E 进度事件
# 事件类型: stage_start, stage_complete, error, final_report
```

#### 3.2 外部 Webhook 分发
**新建**: `backend/agents/channel_dispatcher.py`
```python
# 参考 OpenClaw channels/dock.ts 抽象
# - ChannelDispatcher: register_webhook(url, events=[])
# - dispatch(event, payload): httpx 异步 POST
# - 支持 Telegram Bot API / 企业微信 WebHook
```

### Phase 4 — 系统联调与 E2E 测试 (预计 2 天)

- 完整 Pipeline 压力测试 (模拟真实话题输入)
- 异常恢复测试 (Zep 超时、OASIS 崩溃等)
- 记忆先验偏置验证 (同话题第二次运行应引用历史记忆)
- 全链路性能基线 (Phase 0 到 Phase 6 端到端耗时)

---

## 五、新增文件清单

| 文件路径 | 功能 | 优先级 |
| :--- | :--- | :---: |
| `backend/agents/scheduler.py` | APScheduler 心跳调度引擎 | P0 |
| `backend/agents/memory.py` | SQLite + Zep 长期记忆连接器 | P0 |
| `backend/agents/context_engine.py` | 会话上下文生命周期管理 | P0 |
| `backend/agents/pipeline/e2e_coordinator.py` | E2E 主控协调器 | P1 |
| `backend/agents/subagent_registry.py` | 子智能体注册与生命周期 | P1 |
| `backend/agents/channel_dispatcher.py` | 多渠道推送分发网关 | P2 |

---

## 六、关键设计决策与风险

| 决策 | 选择 | 理由 |
| :--- | :--- | :--- |
| 调度框架 | `APScheduler (AsyncIO)` | 与 FastAPI 生命周期无缝集成 |
| 向量存储 | `SQLite + sqlite-vec` | 参考 OpenClaw, 轻量无基础设施依赖 |
| 图谱存储 | `Zep Cloud` | 支持时序边, 适合舆情演化建模 |
| Agent 通信 | `Forum.log + asyncio.Queue` | 参考 BettaFish, 结合异步化改造 |
| 心跳间隔 | `6小时` (可配置) | 覆盖热点周期, 避免过频触发平台封禁 |

> ⚠️ **风险点**: Zep Cloud API 限速 & OASIS 单机内存瓶颈, 建议设置 `max_agents=50` 和 `timeout=600s` 保护边界
